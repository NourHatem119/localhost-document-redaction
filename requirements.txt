# Synthetic dataset generation
Faker>=25.0.0
# PDF rendering + synthetic image generation for fixtures
reportlab>=4.0.0
pillow>=10.0.0
# DOCX rendering for mixed-format fixtures
python-docx>=1.1.0

# ── Pipeline / redaction ──
PyMuPDF>=1.24.0          # fitz — PDF text extraction + true redaction
requests>=2.31.0         # EXO / Ollama HTTP client

# ── Web UI ──
flask>=3.0.0             # Local web interface for hackathon demo

# Optional: Presidio for structured PII detection (regex is built-in)
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0

# Cognee memory graph (Person B). Pinned to the version verified in T1.
# In-process ladybug (graph) + lancedb (vector) — no DB server, offline.
# [fastembed] = local CPU embeddings, no API key, fully offline.
# Install:  uv pip install "cognee[fastembed]"   (or pip)
cognee[fastembed]==1.1.3
# Pre-download the embedding model WHILE ONLINE so it's cached for airplane mode:
#   python -c "from fastembed import TextEmbedding; TextEmbedding('BAAI/bge-small-en-v1.5')"
